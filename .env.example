# Build Engine Environment Variables
# Copy this to .env and fill in your values

# Required: LLM API Keys
GROQ_API_KEY=
GEMINI_API_KEY=

# Optional: Database (Supabase PostgreSQL)
# If not set, engine uses file-based persistence
DATABASE_URL=

# Engine Configuration
ENGINE_PORT=3001
NODE_ENV=production
